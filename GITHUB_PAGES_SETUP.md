# 🚀 GitHub Pages Deployment Guide

Пошаговая инструкция по развёртыванию проекта на GitHub Pages.

## 📋 Предварительные требования

1. Репозиторий на GitHub
2. Git настроен локально
3. Node.js и npm установлены

## 🔧 Шаг 1: Настройка репозитория

### 1.1. Включи GitHub Pages в настройках репозитория

1. Перейди в **Settings** → **Pages**
2. В разделе **Source** выбери:
   - **Source**: `GitHub Actions`
3. Сохрани изменения

### 1.2. Проверь имя репозитория

Убедись, что имя репозитория совпадает с настройкой в `vite.config.ts`:

```typescript
// Если репозиторий называется "my-population-app"
// base будет автоматически '/my-population-app/'
```

Если имя репозитория отличается от `population-diagram`, обнови `vite.config.ts`:

```typescript
base: process.env.GITHUB_PAGES === 'true' 
  ? '/твое-имя-репозитория/' 
  : '/',
```

## 🔧 Шаг 2: Настройка GitHub Actions

### 2.1. Workflow уже создан

Файл `.github/workflows/deploy.yml` уже настроен и готов к использованию.

### 2.2. Проверь настройки

Убедись, что в `package.json` есть скрипт:

```json
"build:gh-pages": "GITHUB_PAGES=true npm run build"
```

## 🚀 Шаг 3: Деплой

### 3.1. Закоммить и запушить изменения

```bash
git add .
git commit -m "feat: add GitHub Pages deployment"
git push origin main
```

### 3.2. Проверить деплой

1. Перейди в **Actions** вкладку репозитория
2. Дождись завершения workflow `Deploy to GitHub Pages`
3. После успешного деплоя сайт будет доступен по адресу:
   ```
   https://твой-username.github.io/имя-репозитория/
   ```

## 🔄 Автоматический деплой

После настройки каждый push в ветку `main` автоматически:
1. Соберёт проект
2. Задеплоит на GitHub Pages

## 🌐 Custom Domain (опционально)

Если хочешь использовать свой домен:

1. В `vite.config.ts` измени:
   ```typescript
   base: '/',  // Убери GitHub Pages base path
   ```

2. В настройках GitHub Pages добавь свой домен
3. Настрой DNS записи у провайдера домена

## 🐛 Troubleshooting

### Проблема: Сайт показывает 404

**Решение**: Проверь `base` path в `vite.config.ts` — он должен совпадать с именем репозитория.

### Проблема: Assets не загружаются

**Решение**: Убедись, что `base` path правильный и начинается/заканчивается с `/`.

### Проблема: Workflow не запускается

**Решение**: 
1. Проверь, что GitHub Pages включён в Settings → Pages
2. Убедись, что workflow файл в `.github/workflows/`
3. Проверь права доступа в Settings → Actions → General

## 📝 Обновление URL в meta-тегах

После деплоя обнови URL в `index.html`:

```html
<!-- Замени на реальный URL -->
<link rel="canonical" href="https://твой-username.github.io/имя-репозитория/" />
<meta property="og:url" content="https://твой-username.github.io/имя-репозитория/" />
```

## ✅ Проверочный список

- [ ] GitHub Pages включён в Settings → Pages
- [ ] Workflow файл создан (`.github/workflows/deploy.yml`)
- [ ] Имя репозитория совпадает с base path
- [ ] Push в main ветку выполнен
- [ ] Workflow успешно завершился
- [ ] Сайт доступен по URL
- [ ] Все assets загружаются корректно
- [ ] Meta-теги обновлены с правильным URL

---

**Готово! 🎉** Твой сайт теперь доступен на GitHub Pages!
