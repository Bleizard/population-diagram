# 🚀 GitHub Pages Deployment Guide

## Быстрая настройка

### 1. Включи GitHub Pages в настройках репозитория

1. Перейди в **Settings** → **Pages**
2. В разделе **Source** выбери:
   - **Source**: `GitHub Actions`
3. Сохрани изменения

### 2. Обнови base path в vite.config.ts

Если твой репозиторий называется **не** `population-diagram`, обнови строку в `vite.config.ts`:

```typescript
base: process.env.GITHUB_PAGES === 'true' ? '/твой-репозиторий/' : '/',
```

Например, если репозиторий `my-population-app`:
```typescript
base: process.env.GITHUB_PAGES === 'true' ? '/my-population-app/' : '/',
```

### 3. Запусти деплой

Просто запушь изменения в `main` ветку:

```bash
git push origin main
```

GitHub Actions автоматически:
- Соберёт проект
- Задеплоит на GitHub Pages

### 4. Проверь результат

После успешного деплоя твой сайт будет доступен по адресу:
```
https://твой-username.github.io/population-diagram/
```

---

## 🔧 Ручной деплой (альтернатива)

Если хочешь задеплоить вручную:

```bash
# Собери проект с правильным base path
npm run build:gh-pages

# Задеплой через gh-pages пакет
npx gh-pages -d dist
```

Или установи `gh-pages` глобально:
```bash
npm install -g gh-pages
gh-pages -d dist
```

---

## 📝 Обновление URL в meta-тегах

После деплоя обнови URL в `index.html`:

1. Открой `index.html`
2. Замени все вхождения `https://population-pyramid.vercel.app/` на:
   ```
   https://твой-username.github.io/population-diagram/
   ```
3. Также обнови `sitemap.xml` и `robots.txt`

---

## ✅ Проверка после деплоя

- [ ] Сайт открывается по правильному URL
- [ ] Все ресурсы загружаются (CSS, JS, изображения)
- [ ] Роутинг работает (если используется)
- [ ] Meta-теги обновлены
- [ ] Favicon отображается

---

## 🐛 Troubleshooting

### Проблема: 404 на всех страницах

**Решение**: Проверь `base` в `vite.config.ts` — он должен совпадать с именем репозитория.

### Проблема: Ресурсы не загружаются

**Решение**: Убедись, что все пути начинаются с `/population-diagram/` (или твоего имени репозитория).

### Проблема: GitHub Actions не запускается

**Решение**: 
1. Проверь, что в Settings → Pages выбран источник "GitHub Actions"
2. Проверь, что workflow файл находится в `.github/workflows/deploy.yml`
3. Проверь логи в Actions tab

---

## 🌐 Custom Domain (опционально)

Если хочешь использовать свой домен:

1. Добавь файл `CNAME` в `public/` с твоим доменом:
   ```
   example.com
   ```

2. Настрой DNS записи у провайдера:
   - Type: `CNAME`
   - Name: `@` или `www`
   - Value: `твой-username.github.io`

3. В `vite.config.ts` измени base на `/`:
   ```typescript
   base: '/',
   ```

---

**Готово! 🎉**

